<!DOCTYPE html>
<html lang="cs">
<head>
  <meta charset="UTF-8">
  <title>Košík</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body class="bg-light">

  <div class="container mt-4">
    <div class="d-flex justify-content-between mb-3">
      <a href="/" class="btn btn-outline-secondary">Zpět na produkty</a>
      <a href="/logout" class="btn btn-outline-danger">Logout</a>
    </div>

    <h2 class="mb-4">Košík</h2>

    <% if (items.length === 0) { %>
      <div class="alert alert-info">Košík je prázdný.</div>
    <% } else { %>
      <div class="list-group mb-4">
        <% items.forEach(item => { %>
          <div class="list-group-item d-flex justify-content-between align-items-center">
            <div>
              <img src="<%= item.image %>" alt="<%= item.name %>" style="width: 50px; height: auto; margin-right: 10px;">
              <strong><%= item.name %></strong> × <%= item.quantity %><br>
              <small>Cena za kus: <%= (item.finalPrice / item.quantity).toFixed(2) %> Kč</small>
            </div>
            <span><strong><%= item.finalPrice.toFixed(2) %> Kč</strong></span>
          </div>
        <% }); %>
      </div>
      <h4>Celkem: <%= total.toFixed(2) %> Kč</h4>
      <p><em class="text-muted">Objednávky zatím nejsou implementovány.</em></p>
    <% } %>
  </div>

</body>
</html>
